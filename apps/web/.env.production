# TEMP — preview-branch validation only (2026-06-12, feat/web-whatsapp-cta-cold-start).
# The agent session has no Vercel credential, so this stands in for the Vercel
# dashboard env var to validate the WhatsApp CTA on the preview deployment.
# Before merging to main: REMOVE this file and set NEXT_PUBLIC_WHATSAPP_BOT_NUMBER
# in Vercel project settings (Preview + Production scope). A platform env var
# always takes precedence over this file, so setting it early is also safe.
# Value = PingTally's PUBLIC business WhatsApp number (verified_name "PingTally").
NEXT_PUBLIC_WHATSAPP_BOT_NUMBER=972559662581
