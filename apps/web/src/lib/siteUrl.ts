// Canonical public web origin for Pingtally. Single source of truth for
// metadataBase, canonical URLs, robots, sitemap, and JSON-LD. This is the WEB
// origin (pingtally.com) - distinct from the API origin (api.pingtally.com,
// resolved separately via NEXT_PUBLIC_API_URL / lib/apiBase.ts).
export const SITE_URL = "https://pingtally.com";
