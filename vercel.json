{
  "installCommand": "npm install -g pnpm@10.8.1 && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @shopping-assistant/web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
